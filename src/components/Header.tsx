import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="bg-white shadow p-4">
      <h2 className="text-xl font-semibold">Loyalty Dashboard</h2>
    </header>
  );
};

export default Header;